<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head', { title: post.title }) %>
    <link rel="stylesheet" href="/styles/posts.css" />
  </head>

  <body>
    <%- include('includes/header') %>

    <main id="post-detail">
      <h1><%= post.title %></h1>

      <section id="post-meta">
        <address>
          <a href="mailto:<%= post.authour_email %>"><%= post.author_name %></a>
        </address>
        |
        <time datetime="<%= post.date %>"><%= post.humanReadable %> </time>
      </section>
      <hr />
      <section id="post-content">
        <p id="body"><%= post.body %></p>
      </section>
      <a href="/posts" class="btn">← Back to All Posts</a>
    </main>
  </body>
</html>
